<strong>最终结果</strong>：最低的运费为\VAR{result.opt_value}元, 最优运输方案为:
{% from "latex.jinja" import mytabular_preamble as preamble %}
\BLOCK{ for decision in decision_result }
<p align="middle"></br>
<strong>最优运输方案表\BLOCK{ if decision_result|length > 1}\VAR{loop.index}\BLOCK{endif}</strong></br>
{% call latex(compiler="pdflatex", image_format="png", tex_preamble=preamble)  %}
\begin{table}[H]\centering
\begin{mytabular}{c|\BLOCK{for idx in range(1, dp.demand|length + 1)}c\BLOCK{if not loop.last}|\BLOCK{endif}\BLOCK{endfor}}\hline\hline
\diagbox{产地}{运输量}{销地}& \BLOCK{for idx in range(1, dp.demand|length + 1)}$B_{\VAR{idx}}$\BLOCK{if not loop.last}&\BLOCK{endif}\BLOCK{ endfor } \\ \hline
\BLOCK{ for s_idx in range(2) }
$A_{\VAR{s_idx + 1}}$& \BLOCK{ for d_idx in range(dp.demand|length) }$\VAR{decision[s_idx][d_idx]|int}$\BLOCK{if not loop.last}&\BLOCK{endif}\BLOCK{endfor} \\ \hline
\BLOCK{ endfor }
\end{mytabular}
\end{table}
{% endcall %}
</p>
\BLOCK{ endfor }