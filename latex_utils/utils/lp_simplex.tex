\BLOCK{ if simplex_pre_description -}
\VAR{simplex_pre_description}
\BLOCK{ endif }

{% from "latex.jinja" import mytabular_preamble as preamble %}

<p align="middle">
{% call latex(compiler="pdflatex", image_format="png", tex_preamble=preamble)  %}
\begin{keytable}[H]\centering
\topcaption{}
\begin{tabular}{c|c|\BLOCK{ for x in range(0, lp.solve_n_variable) }C{1cm}\BLOCK{ endfor }|c}\hline\hline
\multicolumn{2}{c|}{$c_j$}\BLOCK{ for c in lp.solve_goal_list }&\VAR{c}\BLOCK{ endfor }&\multirow{2}*{$\mathbf{b}$} \\ \cline{1-\VAR{lp.solve_n_variable+2}}
$\mathbf{C_B}$&$\mathbf{X_B}$\BLOCK{ for x in lp.solve_x_list }&\VAR{x}\BLOCK{ endfor }&\\ \hline
\BLOCK{ for i in range(0, lp.tableau_list|length) }
\BLOCK{ for j in range(0, lp.solve_cb_list[i]|length + 1) }
\BLOCK{ if not loop.last } \VAR{lp.solve_cb_list[i][j]}&\VAR{lp.solve_xb_list[i][j]}&\BLOCK{ else } \multicolumn{2}{c|}{$\bar c_j$}& \BLOCK{ endif } \VAR{lp.tableau_list[i][j]|join("&") } \\
\BLOCK{- if loop.revindex0 <= 1 } \hline \BLOCK{ endif }

\BLOCK{ endfor }

\BLOCK{ endfor }

\hline\end{tabular}
\end{keytable}%

{% endcall %}
</p>

\BLOCK{ if simplex_after_description -}
\VAR{simplex_after_description}
\BLOCK{ endif }